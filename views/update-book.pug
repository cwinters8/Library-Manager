extends book-form

block title
  title= book.title

block header
  h1 Update Book
append buttons  
  input.btn.btn-danger(type="submit" form="delete" value="Delete Book")
append body
  form#delete(method="post" action=`/books/${book.id}/delete` onsubmit="return confirm('Do you really want to delete this book?');")